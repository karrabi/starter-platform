info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@test.com",
        "password": "Admin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
